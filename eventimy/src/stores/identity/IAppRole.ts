import { defineStore } from "pinia";


/**
 * Defines Storage Of User Data - JWT Token For Authorization.
 */
const useAppRoleStore = defineStore({
    id: "appRole",
    getters: {},
    actions: {}
});

export default useAppRoleStore;